// Cambiamos el valor de refresh para activar el refrescamiento.
function dec2bin(
    decimal: number
) {
    const bitArray = [];

    return bitArray;
};

function bin2dec(
    bitArray: number[]
) {
    const decimal = 0;
    return decimal;
}

export {
    bin2dec,
    dec2bin
};